'use client'

import React from 'react'

import { LoginForm } from '@/features/auth/components/LoginForm'

function Login() {
  return (
    <div>
      <LoginForm onSuccess={() => {}} />
    </div>
  )
}

export default Login
